---
- name: Install Splunk Enterprise Server
  hosts: seclab-splunk
  vars_prompt:
    - name: splunk_password
      prompt: "Enter Splunk Admin Password"
      private: true
  become: true
  become_user: root
  roles:
    - docker
    - splunk-es
